apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: my-apps-set
  namespace: argocd

spec:
  generators:
    - list:
        elements:
          - appName: quiz-master
            repoURL: https://github.com/Sampanionyy/quiz-master.git
          - appName: pdf-viewer
            repoURL: https://github.com/Sampanionyy/pdf-viewer.git

  template:
    metadata:
      name: '{{appName}}'

    spec:
      project: default

      source:
        repoURL: '{{repoURL}}'
        targetRevision: main
        path: quiz-master

        helm:
          valueFiles:
            - values.yaml

      destination:
        server: https://kubernetes.default.svc
        namespace: default

      syncPolicy:
        automated:
          prune: true
          selfHeal: true