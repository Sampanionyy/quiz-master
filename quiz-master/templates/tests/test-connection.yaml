apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "quiz-master.fullname" . }}-test-connection"
  labels:
    {{- include "quiz-master.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "quiz-master.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
