apiVersion: v1
kind: Service
metadata:
  name: {{ include "quiz-master.fullname" . }}
  labels:
    {{- include "quiz-master.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "quiz-master.selectorLabels" . | nindent 4 }}
