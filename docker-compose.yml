version: "3.8"

services:
    quiz-master:
        build: .
        ports:
            - "5000:5000"
        restart: always
        volumes:
            - ./app:/app/app

    test:
      build: .
      volumes:
        - .:/app
      working_dir: /app
      command: pytest -v